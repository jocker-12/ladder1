<!--index.wxml-->
<view class="container">
  <view wx:for='{{list}}' wx:key='{{index}}' data-index="{{index}}" class='list' bindtap='toInfo'>
    <image></image>
    <view>
        <text>{{item.author}}</text>
        <text>{{item.name}}</text>
    </view>
  </view>
</view>
