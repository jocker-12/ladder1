import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/index.jsx';
ReactDOM.render( < App data = { 100 }
        pageSize = { 10 }
        / > , document.getElementById('root'));